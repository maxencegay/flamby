	/*Create table account*/
CREATE TABLE IF NOT EXISTS account (id int UNIQUE, name varchar(50), fam_name varchar(50), mail varchar(100) UNIQUE, password varchar(50), city varchar(50), region varchar(50), department varchar(50))

CREATE SEQUENCE IF NOT EXISTS puvlic.account_id START 1 INCREMENT 1 MINVALUE 1 OWNED BY public.account.id


	/*Create table advert*/
CREATE TABLE IF NOT EXISTS advert (id int UNIQUE, type varchar(50), title varchar(100), description varchar(1000), address varchar(200), city varchar(50), region varchar(50), department varchar(50), mnemonic varchar(50), account_id int)

CREATE SEQUENCE IF NOT EXISTS advert_id START 1 INCREMENT 1 MINVALUE 1 OWNED BY advert.id



	/*Create account*/
INSERT INTO account (id, name, fam_name, mail, password, city, region, department) VALUES (nextval('account_id'),'Jul', 'RPZ', 'Jul.RPZ@gmail.com', '123456', 'Beynost', 'Rhône-Alpes', 'Ain');


	/*Create advert*/
INSERT INTO advert (id, type, title, description, address, city, region, department, mnemonic, account_id) VALUES (nextval('advert_id'), 'lost', 'Cat', 'Where is my Cat ?', 'église saint julien', 'Beynost', 'Rhône-Alpes', 'Ain', 'cat_id0101', 1);



